<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.gujun.android.taggroup.demo">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.SecondActivity" />
        <activity android:name=".ui.FlexBoxActivity" />
        <activity android:name=".ui.RecyclerViewSnapHelpActivity" />
        <!-- 网络监听 start -->
        <!-- <receiver android:name=".util.NetWorkStateReceiver"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.net.conn.CONNECTIVITY_CHANGE" /> -->


        <!-- <category android:name="android.intent.category.DEFAULT" /> -->
        <!-- </intent-filter> -->
        <!-- </receiver> -->
        <!-- 网络监听 end -->
        <activity
            android:name=".ui.bottom.BottomTestActivity"
            android:label="@string/title_activity_bottom_test" />
        <activity android:name=".ui.FrescoActivity"></activity>
    </application>

</manifest>