<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.gujun.android.taggroup.demo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:name=".App"
        >
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.TagEditorActivity"
            android:label="@string/title_activity_tag_editor"
            android:parentActivityName=".ui.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="me.gujun.android.taggroup.demo.ui.MainActivity" />
        </activity>
        <activity android:name=".ui.SecondActivity"
            />
        <activity android:name=".ui.FlexBoxActivity" />
        <activity android:name=".ui.RecyclerViewSnapHelpActivity"></activity>
    </application>

</manifest>